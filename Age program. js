let age=20;
// ">" is greater than,"<" is lesser than,">=" greater than or equals to,"<=" less than or equals to
if(age>=18)
    {
        console.log("You are eligible to vote")
    }
else{
    console.log("Sorry You can't vote")
}
/*
if(condition)
{
    //code to runif the condition is true.
}
else{
    //code to run if the condition is false.
}
*/
